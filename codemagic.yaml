workflows:
  ios-workflow:
    name: iOS Workflow
    max_build_duration: 120
    instance_type: mac_mini_m1
    environment:
      groups:
        - "2"  # Supabase environment variables grubu (VITE_SUPABASE_URL ve VITE_SUPABASE_ANON_KEY)
      vars:
        XCODE_WORKSPACE: "ios/App/App.xcworkspace"
        XCODE_SCHEME: "App"
        BUNDLE_ID: "com.esnaftaucuz.app"
        APP_ID: "com.esnaftaucuz.app"
      node: 18
      xcode: latest
      cocoapods: default
    scripts:
      - name: Install dependencies
        script: |
          npm install
      - name: Build web
        script: |
          # Environment variables must be set in Codemagic UI (Settings → Environment variables)
          # VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY should be added as encrypted variables
          echo "🔍 Checking environment variables..."
          echo "VITE_SUPABASE_URL is set: $([ -n "$VITE_SUPABASE_URL" ] && echo 'YES ✅' || echo 'NO ❌')"
          echo "VITE_SUPABASE_ANON_KEY is set: $([ -n "$VITE_SUPABASE_ANON_KEY" ] && echo 'YES ✅' || echo 'NO ❌')"
          
          # Check if environment variables are set
          if [ -z "$VITE_SUPABASE_URL" ] || [ -z "$VITE_SUPABASE_ANON_KEY" ]; then
            echo "❌ ERROR: Environment variables are missing!"
            echo "Please add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY in Codemagic Settings → Environment variables"
            echo "Build will fail without these variables."
            exit 1
          fi
          
          # Export environment variables explicitly for npm build
          # Vite automatically reads VITE_* prefixed environment variables from process.env
          export VITE_SUPABASE_URL="$VITE_SUPABASE_URL"
          export VITE_SUPABASE_ANON_KEY="$VITE_SUPABASE_ANON_KEY"
          
          echo "✅ Environment variables are set, starting build..."
          echo "VITE_SUPABASE_URL: ${VITE_SUPABASE_URL:0:30}..."
          echo "VITE_SUPABASE_ANON_KEY: ${VITE_SUPABASE_ANON_KEY:0:20}..."
          
          # Verify environment variables are accessible
          echo "🔍 Verifying environment variables are accessible to Node.js..."
          node -e "console.log('VITE_SUPABASE_URL:', process.env.VITE_SUPABASE_URL ? 'SET ✅' : 'MISSING ❌'); console.log('VITE_SUPABASE_ANON_KEY:', process.env.VITE_SUPABASE_ANON_KEY ? 'SET ✅' : 'MISSING ❌');"
          
          # Build with environment variables - Vite will automatically inject them
          npm run build
          
          # Verify build output contains correct URL (not placeholder)
          if grep -r "placeholder.supabase.co" dist/ 2>/dev/null; then
            echo "❌ WARNING: Build output still contains placeholder URL!"
            echo "This means environment variables were not properly injected."
            echo "Checking if Supabase URL is in build output..."
            grep -r "xmskjcdwmwlcmjexnnxw" dist/ 2>/dev/null | head -1 || echo "❌ Supabase URL not found in build output"
            exit 1
          else
            echo "✅ Build verification: No placeholder URLs found in build output"
            echo "✅ Checking if correct Supabase URL is in build output..."
            if grep -r "xmskjcdwmwlcmjexnnxw" dist/ 2>/dev/null | head -1; then
              echo "✅ Correct Supabase URL found in build output!"
            else
              echo "⚠️ Supabase URL not found in build output (might be minified)"
            fi
          fi
      - name: Add iOS platform if not exists
        script: |
          if [ ! -d "ios" ]; then
            echo "iOS platform not found, adding..."
            npx cap add ios
          else
            echo "iOS platform already exists"
          fi
      - name: Install CocoaPods dependencies
        script: |
          cd ios/App
          pod install
          cd ../..
      - name: Sync Capacitor
        script: |
          npx cap sync ios
      - name: Set up code signing settings on Xcode project
        script: |
          xcode-project use-profiles
      - name: Build iOS app
        script: |
          xcodebuild build \
            -workspace "$XCODE_WORKSPACE" \
            -scheme "$XCODE_SCHEME" \
            -configuration Release \
            -destination generic/platform=iOS \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            -derivedDataPath "$CM_BUILD_DIR/build"
      - name: Create IPA from build
        script: |
          echo "Creating IPA from build..."
          mkdir -p build/ios/ipa
          
          # Find the .app bundle
          APP_BUNDLE=$(find "$CM_BUILD_DIR/build" -name "App.app" -type d | head -1)
          
          if [ -z "$APP_BUNDLE" ]; then
            # Try alternative paths
            APP_BUNDLE=$(find "$CM_BUILD_DIR/build" -name "*.app" -type d | head -1)
          fi
          
          if [ -n "$APP_BUNDLE" ]; then
            echo "Found app bundle: $APP_BUNDLE"
            # Create IPA structure
            mkdir -p build/ios/ipa/Payload
            cp -R "$APP_BUNDLE" build/ios/ipa/Payload/
            
            # Create IPA file
            cd build/ios/ipa
            zip -r "$APP_ID.ipa" Payload
            cd ../../..
            
            echo "IPA created: build/ios/ipa/$APP_ID.ipa"
            ls -lh build/ios/ipa/
          else
            echo "App bundle not found. Searching in build directory..."
            find "$CM_BUILD_DIR/build" -type d -name "*.app" || echo "No .app bundles found"
            # Create empty structure for debugging
            mkdir -p build/ios/ipa
            echo "IPA creation failed - app bundle not found" > build/ios/ipa/error.txt
          fi
    artifacts:
      - build/ios/ipa/*.ipa
      - build/ios/ipa/**/*
      - $CM_BUILD_DIR/build/**/*.app
      - $CM_BUILD_DIR/build/**/*.xcarchive
    publishing:
      email:
        recipients:
          - your-email@example.com
        notify:
          success: true
          failure: false
      scripts:
        - name: Print build info
          script: |
            echo "Build completed!"
            echo "Bundle ID: $BUNDLE_ID"
            echo "Workspace: $XCODE_WORKSPACE"
            echo "Scheme: $XCODE_SCHEME"

